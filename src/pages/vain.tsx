const Vain = () => {
    const style: Record<string, any> = {
        backgroundColor: "transparent",
        color: "white"
    };

    return <div style={style}>Vain</div>;
};

export default Vain;
